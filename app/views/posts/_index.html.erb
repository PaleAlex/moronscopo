<!DOCTYPE html>
<html lang="en">
<head>
	<title>Moronscope | Home</title>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">

	<link href="https://fonts.googleapis.com/css?family=Poppins:200,200i,300,300i,400,400i,500,500i,600,600i,700,700i,800,900&display=swap" rel="stylesheet">
	<link href="https://fonts.googleapis.com/css2?family=Comic+Neue:ital,wght@0,300;0,400;0,700;1,300;1,400;1,700&display=swap" rel="stylesheet">
	<link href="//maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">

</head>
<body>
<!-- Zodiac Sign -->
<div id="zodiac_Sign" class="rashi_wrapper">
	<div class="container-fluid">
		<div class="heading_wrapper wow fadeInDown animated">
			<h2 class="wow fadeInDown animated">Oroscopo della settimana </h2>
      <table>
        <tbody>
          <% if @posts.count > 0 %>
            <% @posts.each do |post| %>
              <tr>
                <i> Aggiornato il giorno <%= post.updated_at.strftime("%d %b") %> </i><br> <br>
                <%= post.description %> <br><br>
                <%= link_to 'Aggiorna MoronScopo', edit_post_path(post.id), class: "btn btn-secondary btn-sm" %>
              </tr>
            <% end %>
          <% end %>
        </tbody>
      </table>
		</div>
  </div>
</div>

<br>

<%= link_to 'Aggiorna MoronScopo', new_post_path %> |
<%= link_to 'Back to home', root_path %>
